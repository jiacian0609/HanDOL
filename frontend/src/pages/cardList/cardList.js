import axios from 'axios';
import { useState } from 'react';
import './cardList-style.js';
import { IndexWrapper, IndexTitle, IndexDivider, IndexForm, IndexButtons, IndexButton, IndexFormField, IndexText, IndexInput, IndexSubmit } from './cardList-style.js';

export default function CardList() {
    return (
        <IndexWrapper>
        </IndexWrapper>
    )
}